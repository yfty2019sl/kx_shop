import _extends from "@babel/runtime/helpers/esm/extends";
import { ref, mergeProps, createVNode } from "vue";
import { createNamespace } from '../utils';
import { useExpose } from '../composition/use-expose';
import TimePicker from './TimePicker';
import DatePicker from './DatePicker';

var _createNamespace = createNamespace('datetime-picker'),
    createComponent = _createNamespace[0],
    bem = _createNamespace[1];

export default createComponent({
  props: _extends({}, TimePicker.props, DatePicker.props),
  setup: function setup(props, _ref) {
    var attrs = _ref.attrs;
    var root = ref();
    useExpose({
      getPicker: function getPicker() {
        var _root$value;

        return (_root$value = root.value) == null ? void 0 : _root$value.getPicker();
      }
    });
    return function () {
      var Component = props.type === 'time' ? TimePicker : DatePicker;
      return createVNode(Component, mergeProps({
        "ref": root,
        "class": bem()
      }, _extends({}, props, attrs)), null);
    };
  }
});